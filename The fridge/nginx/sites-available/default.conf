server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;
    root /var/www/html;
    index index.php;

    include /etc/nginx/default_site_params;
    access_log off;
    error_log off;
}
